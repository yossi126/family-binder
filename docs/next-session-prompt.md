# פרומפט לסשן הבא — בדיקות S5 וכיבוי הבוט

להעתיק ולהדביק בסשן חדש של Claude Code שנפתח בתיקייה
`C:\Users\yossi.br\OneDrive - Top Group\Documents\GitHub\family-binder`:

---

קרא את `CLAUDE.md`, ואז את `docs/handoff.md` ו-`docs/progress.md`. **חזור לי בשורה אחת על הצעד הפנדינג** לפני שאתה נוגע במשהו.

מצב: Phases 0–4 הושלמו ואומתו חי. האתר חי ב-GitHub Pages, ה-backend ב-Apps Script, 13 תורים מהבוט כבר מיובאים. נשארו **S5** (בדיקה באייפון) ו-**Phase 5 / S6** (כיבוי הבוט).

כללי עבודה לסשן הזה:
- מותר `git commit` + `git push` בלי לשאול. Backend: `clasp push --force && clasp create-deployment -i <id>` — **לא** `clasp deploy`. ה-id ב-`docs/handoff.md`.
- אין שמות / פרטים אמיתיים בצ'אט, בקוד, בדוקומנטציה או בצילומי מסך שנכנסים ל-git. ספירות בלבד. צילום מסך עם נתוני אמת — למחוק, לא לקמט.
- ה-PIN לא עובר בצ'אט. אם צריך אותו — הוא ב-`backend/.pin` (git-ignored).
- **אין שינויי עיצוב** בלי שאני מבקש. אם אני מבקש — 2-3 אופציות בטקסט, ואישור, לפני קוד.
- כל תיקון מסתיים באימות חי ובעדכון `docs/progress.md`.

השלב הראשון — S5. תן לי את רשימת הבדיקות באייפון ותחכה. אחרי שאחזיר תוצאות:
- אם הכול עבד → Phase 5: הנחה אותי לגבות את `data/family_agent.db` מה-VM ל-`migration/`, ואז בקש אישור S6 לכיבוי `family-agent`. אחרי אישור — README קצר למשתמש הקצה, handoff סופי.
- אם משהו נשבר → תקן, פרוס, אמת חי, ורק אז חזור לרשימת S5.

---

## רשימת S5 (מה אני בודק באייפון)

1. פתח https://yossi126.github.io/family-binder/ — **רענן חזק**, הקאש שומר CSS ישן.
2. הקלד PIN → אמורים להופיע 13 תורים בפילטר "הכול" (4 ב"הקרובים").
3. שיתוף ⬆️ → "הוסף למסך הבית" → פתח מהאייקון: מסך מלא, בלי סרגל דפדפן, אייקון כחול.
4. פתח תור → "העלאת מסמך" → **צלם תמונה** → ודא שמופיעה ברשימה, ש"פתח" מציג אותה, ואחר כך מחק אותה (סמל פח).
5. הוסף הערה ומחק אותה.
6. מחוות "חזרה" (החלקה מהקצה) מתוך פרטי תור → חוזרת לרשימה, לא יוצאת מהאתר.
7. במחשב: שתף את תיקיית הדרייב ואת היומן עם האחות; שלח לה URL + PIN (לא כאן).

**מה להחזיר:** `iphone: ok` או פירוט מה נשבר (צילום מסך בלי נתונים אמיתיים, או תיאור), וכמה תורים ראית.
