# פרומפט פתיחה לסשן המימוש

להעתיק ולהדביק בסשן חדש של Claude Code שנפתח בתיקייה
`C:\Users\yossi.br\OneDrive - Top Group\Documents\GitHub\family-binder`:

---

קרא את `CLAUDE.md` ואת `docs/plan.md` ותתחיל לממש את הפרויקט לפי השלבים, מ-Phase 0.

כללי עבודה לסשן הזה:
- עבוד באופן אוטונומי. עצור **רק** בנקודות S1–S6 שבתוכנית (יצירת repo בגיטהאב, `clasp login`, הרצת `setup()` והגדרת PIN, הפעלת GitHub Pages, בדיקה באייפון, אישור כיבוי הבוט). בכל נקודה כזו תן לי הוראות מדויקות מה לעשות ומה להחזיר לך, וחכה.
- בפרויקט הזה מותר לך לבצע `git commit` ו-`git push` בלי לשאול — זה חלק מהפריסה.
- כל שלב מסתיים באימות חי כמו שמוגדר בתוכנית, ובעדכון `docs/progress.md`. דווח לי בסוף כל שלב ב-3-5 שורות: מה עובד, מה אומת, מה השלב הבא.
- אל תכתוב שמות או פרטים אמיתיים מהנתונים בצ'אט, בקוד או בדוקומנטציה — רק ספירות.
- ה-PIN: אני אגדיר אותו בעצמי ב-Script Properties; אם אתה צריך אותו ל-curl, בקש ממני לשמור אותו בקובץ מקומי git-ignored ואל תבקש אותו בצ'אט.
- אם משהו בתוכנית מתגלה כלא נכון בפועל (API של גוגל, מגבלה), עצור, הצע תיקון קצר לתוכנית, ואחרי אישורי המשך.

---
